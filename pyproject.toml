[project]
name = "wiktbot"
version = "0.1.0"
description = "A bot for the japanese wiktionary"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pywikibot",
    "pytest",
]

[project.scripts]
wb = "wiktbot.cli:main"
